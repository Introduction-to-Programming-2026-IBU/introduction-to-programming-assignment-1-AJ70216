sort1 uses: bubble sort

How do you know?: It is much slower on reversed lists, and faster on sorted lists.

sort2 uses: merge sort

How do you know?: It is much faster than the others on large inputs.

sort3 uses: selection sort

How do you know?: It takes about the same time regardless of whether the list is sorted, reversed, or random.
